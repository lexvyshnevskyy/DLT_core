/**:
  ros__parameters:
    measurement_topic: ltm2985/measurement
    hmi_commands_topic: /hmi/commands
    database_service: /database/query
    enable_database_client: false
    enable_pwm_controller: false

    pwm_pin: 18
    pwm_frequency_hz: 10
    pwm_range: 1000

    control_channel: 9
    monitor_channel: 3
    target_k: 373.15
    control_period_sec: 1.0
    measurement_timeout_sec: 5.0

    kp: 25.0
    ki: 0.08
    deadband_k: 0.3
    max_output_step: 60
